# Runtime dependencies.
# Deployment target is Windows ARM64: check that a win_arm64 wheel exists
# before adding anything new here (this already ruled out soxr).
# numpy's FIRST win_arm64 wheel is 2.3.0 -- there is none for 2.0-2.2. A looser
# pin means the ARM64 target silently installs a numpy the tests never ran
# against. Verified: `pip download --platform win_arm64 numpy==2.0.2` fails.
numpy>=2.3
sounddevice>=0.5.5   # 0.5.5 ships win_arm64
PySide6>=6.11        # 6.11.1 ships win_arm64 (cp310-abi3)
